/*
 * Copyright (C) 2014- Shingo OKAWA.
 */
#include <cmath>

/* All operations and constants evaluate in the range and
   precision of long double.  */
#if defined(ENABLE_FLT_EVAL_METHOD)
#  define FLT_EVAL_METHOD 2
#endif

/* Detects platform characteristics. */
int main(int argc, char *argv[]) {
  float_t x = 0.0;
  return 0;
}

